<?php

// APIブートストラップ変数は、ルートファイルと意図的に共有されています。
// 既存のコードベースはまだ名前空間化されていないため、ルートファイルでは以下の変数を使用します。
// $path、$method、$db、$appRepo、$releaseRepo、$developerRepo、
// $certificateRepo、$certificateAuthority、$storage、$appConfig、$limit、$offset。